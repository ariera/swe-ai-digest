do we need to keep the --scheduled flag. Should that not be the default behaviour? If anything the flag that makes sense is omething like a --force-resending-campaing (I'm not suggesting we implement it, just point out what the default behavour should be)

> The sources table gets a last_fetched_at and last_error column
I like that idea. Also emailing to admin

>  4. --dry-run vs --no-email
agree to your suggestions


---

Let's discuss about my questions above and then create a new PLAN-v03.md that i will reivew.